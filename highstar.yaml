name: promptbeat-lite
one_liner: Run lightweight prompt regression checks from JSON fixtures.
target_user: Prompt and LLM app engineers
pain_point: Prompt changes need cheap regression checks before full eval suites.
demo_command: PYTHONPATH=src python3 -m promptbeat_lite examples/cases.json
topics:
  - ai-tools
  - prompt-engineering
  - evals
  - testing
  - cli
  - llmops
release_channel:
  - github
  - relevant-developer-communities
launch_copy:
  - I built promptbeat-lite: Run lightweight prompt regression checks from JSON fixtures.
  - The pain: Prompt changes need cheap regression checks before full eval suites.
  - Try it with: PYTHONPATH=src python3 -m promptbeat_lite examples/cases.json
quality_gate:
  commands:
    - python3 -m unittest discover -s tests
metrics_goal:
  day_1_stars: 5
  day_3_stars: 15
  day_7_stars: 50
